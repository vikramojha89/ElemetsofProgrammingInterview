/*
  This program performs add operation on two possitive numbers without using 
  + operator, Or we can say Sum of two numbers without using + operator
 */
 
 #include<stdio.h>
 
 //Main function to perform Addition f two numbers
 unsigned int AddOperation(int a, int b)
 {
   int temp_a=a, temp_b=b, carryin=0 \
       sum = 0;
       
   while(temp_a || temp_b)
   {
     int ak = 
 }     
       
 //Driver code      
 int main()
 {
   unsigned int a, b;
   printf("Get two values of A and B");
   scanf("%d %d", &a, &b);
   AddOperation(a, b);
 }
